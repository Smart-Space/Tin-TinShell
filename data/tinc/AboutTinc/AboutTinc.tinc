/这个文件主要对tinc拓展文件提出要求

/除了说明文件和测试文件，tinc拓展除了要满足命名规范以外，文本内部命令还要有如下规范：

/任何一个拓展文件都要有开头函数，这个函数一般不会被调用，但是需要显示该文本的函数构造
/开头函数全部大写：文本拓展路径_INIT []
/具体如下（文本路径以data\tinc\后开头，中间用“_”连接），以本文本为例
$ ABOUTTINC_ABOUTTINC_INIT []{
#首先是说明，可以不写
#print [（说明）]
#然后是函数结构，必须写
#结构为全小写目录加文件后缀名
#“-”后加函数
#如果有函数调用另一个本地函数而不是用“%”调用，需要在函数后加依赖
#-Function1--need1--need2--...
print [abouttinc\abouttinc.tinc]
print [-]
}